name: wat_schedule
description: "Military University of Technology Mobile schedule app"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  bloc: ^9.0.0
  flutter_bloc: ^9.1.1
  hydrated_bloc: ^10.1.1
  path_provider: ^2.1.5

  equatable: ^2.0.7
  injectable: ^2.5.0
  get_it: ^8.0.3
  
  freezed: ^3.1.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  
  either_dart: ^1.0.0
  dio: ^5.7.0
  
  dropdown_search: ^6.0.2
  skeletonizer: ^2.1.0+1
  go_router: ^16.0.0
  dartz: ^0.10.1

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner: ^2.4.14
  injectable_generator: ^2.6.2
  json_serializable: ^6.9.0

flutter:
  uses-material-design: true
